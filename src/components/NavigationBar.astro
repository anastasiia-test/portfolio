<header>
    <nav class="navbar slightly-below-nav">
        <a href="/" class="nav-branding">Portfolio</a>
        <ul class="nav-menu">
            <a href="#skills">
                <li class="nav-item">
                    <div class="nav-link">Skills</div>
                </li></a
            >
            <a href="https://github.com/anastasiia-test" target="_blank">
                <li class="nav-item">
                    <div class="nav-link">GitHub</div>
                </li></a
            ><a href="#projects">
                <li class="nav-item">
                    <div class="nav-link">Projects</div>
                </li></a>

            <a href="#about">
                <li class="nav-item">
                    <div class="nav-link">About</div>
                </li></a
            >
            <a href="#contact">
                <li class="nav-item">
                    <div class="nav-link">Contact</div>
                </li></a
            >
        </ul>
        <div class="hamburger">
            <span class="bar"></span>
            <span class="bar"></span>
            <span class="bar"></span>
        </div>
    </nav>
</header>
<script src="../scripts/script.js"></script>

<style>
    * {
        padding: 0;
        margin: 0;
        box-sizing: border-box;
        border: 0;
    }
    header {
        position: sticky;
        top: 0;
        margin-top: 0;
        margin: auto;
        width: 100%;
        height: 63px;
        background-color: #262626;
        border-radius: 0;
        z-index: 4;
        border: 0px;
        display: flex;
    }
    li {
        list-style: none;
    }
    a {
        color: white;
        text-decoration: none;
    }
    .navbar {
        height: 63px;
        display: flex;
        justify-content: space-between;
        align-items: center;
        padding: 0 24px;
        width: 100%;
        margin: 0;
        border-radius: 0%;
        border: 0px;
    }
    .nav-menu {
        display: flex;
        justify-content: space-between;
        align-items: center;
        gap: 60px;
    }
    .nav-branding {
        font-size: 2rem;
    }
    .nav-link {
        transition: 0.5s ease;
    }

    .nav-link:is(:hover, :focus-within) {
        color: rgb(var(--accent-light), 89%);
    }
    .hamburger {
        display: none;
        cursor: pointer;
    }
    .bar {
        display: block;
        width: 25px;
        height: 3px;
        margin: 5px;
        -webkit-transition: all 0.3s ease-in-out;
        transition: all 0.3s ease-in-out;
        background-color: white;
    }

    @media (max-width: 768px) {
        .hamburger {
            display: block;
        }
        .hamburger.active .bar:nth-child(2) {
            opacity: 0;
        }
        .hamburger.active .bar:nth-child(1) {
            transform: translateY(8px) rotate(45deg);
        }
        .hamburger.active .bar:nth-child(3) {
            transform: translateY(-8px) rotate(-45deg);
        }
        .nav-menu {
            position: fixed;
            right: -100%;
            top: 62px;
            gap: 0;
            flex-direction: column;
            width: 100%;
            text-align: center;
            transition: 0.3s;
            z-index: 3;
        }
        .nav-menu > a {
            display: table-cell;
            text-align: inherit;
            font-size: medium;
            height: 50px;
            width: 100%;
            padding: 14px 0px 0px 0px;
            background-color: #262626;
        }
        .nav-menu > a:hover {
            background-color: rgba(221, 3, 255, 0.827);
        }
        ul > a:is(:hover, :focus-within) {
            color: white;
        }
        .nav-link:is(:hover, :focus-within) {
            color: white;
        }
        .nav-link {
            transition: 0s;
        }
        .nav-item {
            margin: 0;
        }
        .nav-menu.active {
            right: 0;
        }
    }
</style>
